export default function OrderBook() {
  return (
    <div className="flex flex-col h-full border-l border-r border-[#2a2a2a]">
      <p className="text-white p-4">order book</p>
    </div>
  );
  // TODO: if user does not have a session disable interactivity on order book
}
